#include <iostream>
#include <string>

#include "ScheduleSolver.hpp"

using namespace std;

int main() {
	s = std::string("data/example-inputs.txt");
	ScheduleSolver s = ScheduleSolver(s);
	s.solve();
}
